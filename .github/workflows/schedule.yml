name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ d2b5afea81cdc6c782de901dbbbaa8b9 }}
          TIMEZONE: Asia/Seoul
